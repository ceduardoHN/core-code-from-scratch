```
Algoritmo boolean
	a <- 5 == 3
  //a=FALSO. Ya que se esta comparando si 5 es igual a 3.
	b <- 4 <> 3
  //b=VERDADERO. Ya que se esta comparando si 4 es diferente de 3/
	c <- 7 > 7
  //c=FALSO. Se esta comparando si 7 es mayor al propio 7.
	d <- 4 < 4
  //d=FALSO. Se esta comparando si 4 es menor al propio 4.
	e <- 100 <= 90
  //e=FALSO. Se esta comparando si 100 es menor o igual a 90.
	f <- 40 >= 40
  //f=VERDADERO. Se esta comparando si 40 es mayor o igual al propio 40.
FinAlgoritmo
```
